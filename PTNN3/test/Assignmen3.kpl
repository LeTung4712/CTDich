Program Example2; (* Factorial *)
   
Var n : Integer;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
  End;

Function G(n : Integer) : Integer;
  Begin
    F:=n*n; (*error "An identifier expected." vì gán f bên ngoài phạm vi hàm f*)
  End;

Begin
  For n := 1 To 7 Do
    Begin
      Call WriteLn;
      Call WriteI( F(n));
    End;
End. (* Factorial *)